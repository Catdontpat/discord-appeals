const { Events, ModalBuilder, ActionRowBuilder, TextInputBuilder, TextInputStyle, EmbedBuilder } = require('discord.js');
const appealData = require('../data/appealData')

module.exports = {
    name: Events.InteractionCreate,
    async execute(interaction) {
        
    }
}